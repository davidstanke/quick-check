name: Deploy to prod

on:
    push:
        branches:
            - main
        paths-ignore:
            - "docs/**"
            - "adrs/**"
            - ".gemini/**"
            - "**/README.md"
    schedule:
        - cron: "0 7 * * *"
    workflow_dispatch:

defaults:
    run:
        shell: bash

jobs:
    deploy-service:
        runs-on: ubuntu-latest
        permissions:
            contents: "read"
            id-token: "write"
        steps:
            - uses: "actions/checkout@v6"

            - uses: "google-github-actions/auth@v3"
              with:
                  workload_identity_provider: "projects/55325855002/locations/global/workloadIdenti\
                      tyPools/github-actions/providers/github-provider"
                  service_account: 'quick-check@dev-sandbox-davidstanke.iam.gserviceaccount.com'

            - id: "deploy"
              uses: "google-github-actions/deploy-cloudrun@v3"
              with:
                  service: "hello-cloud-run"
                  source: "."
                #   image: "us-docker.pkg.dev/cloudrun/container/hello:latest"
                  project_id: "dev-sandbox-davidstanke"

            - name: "Use output"
              run: "curl \"${{ steps.deploy.outputs.url }}\""
